import React from 'react';
import Header from '@strata/tempo/lib/header';

const ChargeAllocation: React.FC = () => {
  return <Header title='Charge Allocation' />;
};

export default ChargeAllocation;
