import React from 'react';
import Header from '@strata/tempo/lib/header';

const CostComponents: React.FC = () => {
  return <Header title='Cost Components' />;
};

export default CostComponents;
