import React from 'react';
import Header from '@strata/tempo/lib/header';

const ActivityCodeDesigner: React.FC = () => {
  return <Header title='Activity Code Designer' />;
};

export default ActivityCodeDesigner;
