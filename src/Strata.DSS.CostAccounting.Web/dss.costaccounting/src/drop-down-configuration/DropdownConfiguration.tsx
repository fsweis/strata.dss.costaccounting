import React from 'react';
import Header from '@strata/tempo/lib/header';

const DropdownConfiguration: React.FC = () => {
  return <Header title='Drop-down Configuration' />;
};

export default DropdownConfiguration;
