import React from 'react';
import Header from '@strata/tempo/lib/header';

const OverheadAllocation: React.FC = () => {
  return <Header title='Overhead Allocation' />;
};

export default OverheadAllocation;
